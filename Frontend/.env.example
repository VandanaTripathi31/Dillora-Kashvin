# When the backend is ready, point the frontend at it:
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# The admin dashboard URL (used by the footer "Admin login" link):
NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3001
